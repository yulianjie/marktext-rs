# MarkText 界面设计规范

本轮实现以 [第二版设计稿](design/marktext-glass-editor-v2.png) 为视觉依据。第一版只用于历史对照；第二版明确要求顶部标题与菜单合并、不显示文章路径、保留工具栏搜索入口。

## 1. 目标与布局

使用项目现有的 **Tauri 2 + Vue 3 + Pinia + Muya** 实现可编辑、可导航的桌面界面。Windows 编辑窗口采用自定义标题栏，正文仍由原有 Muya 引擎渲染。

从上到下的结构：

1. **56px 顶栏**：应用图标、MarkText、文件/编辑/段落/格式/视图/主题/窗口/帮助菜单，以及最小化、最大化/还原、关闭。
2. **左侧导航**：约 64px 活动栏与默认 320px 的内容面板，首次进入显示文章目录。
3. **右侧编辑区**：52px 标签栏、54px 格式工具栏、可滚动正文、底部状态栏。
4. 左侧面板与右侧编辑区保留约 5px 间距及 16px 圆角。

文章路径不占用顶部空间，不渲染面包屑。文件名仍显示于标签页；文件操作与标签页原有路径提示继续可用。

## 2. 统一标题栏

- 应用图标跟随已有 appIcon 偏好设置；默认使用蓝色玻璃图标。
- 标题和菜单必须处于同一行，避免系统标题栏、独立菜单栏、路径栏重复占据高度。
- 菜单按钮打开现有 Tauri 原生子菜单，复用完整菜单项目、最近文件、快捷键和格式勾选状态。
- Windows 编辑窗口隐藏已附加的原生菜单栏，并关闭系统装饰；不删除菜单对象。
- 菜单在语言、主题、快捷键或最近文件变化后重建时，再次隐藏菜单栏。新建编辑窗口沿用相同规则。
- 关闭按钮调用窗口 close，进入现有未保存文档确认流程；不得直接销毁窗口或绕过草稿保存。
- 品牌区和菜单右侧空白区为拖动区域，交互按钮不能被拖动区覆盖。
- 菜单支持 Tab 进入、左右方向键移动、Enter/Space/下方向键打开以及 Alt+字母访问。
- 小窗口隐藏菜单括号中的助记字母，保留菜单文字和窗口按钮。
- 设置窗口继续使用系统装饰；macOS/Linux 保留原生标题栏与菜单。此版本的合并顶栏针对 Windows。
- 浏览器预览显示同一标题栏，窗口按钮禁用；点击菜单会说明需要 Tauri 桌面环境，避免模拟原生功能。

## 3. 文章目录

### 视觉规则

- 顶部显示“文章目录”、真实标题数量和收起按钮。
- 标题搜索框放在目录树之前；与工具栏的正文查找区分。
- H1–H6 使用小尺寸级别标签，不使用重复实心圆点表示所有层级。
- 缩进根据实际父子关系计算，每级 16px；跳级标题不会产生大块无意义留白。
- 子级使用细竖线和短横线辅助识别层级；有子项的标题提供展开/折叠箭头。
- 常规目录行最小高度 36px，文字 14px；根标题 15px、较高字重。
- 选中项使用主题强调色的半透明底色、左侧 3px 指示条和较深文字，不能仅依靠颜色表达选中状态。
- 长标题默认省略显示，悬停可查看全文；保留现有“目录文字换行”偏好。
- 树内容独立滚动，搜索框和阅读进度固定在面板顶部和底部。

### 交互规则

- 搜索不区分大小写，显示匹配标题及其祖先路径。
- 搜索时临时展示命中的折叠分支，清空查询后恢复之前的折叠状态。
- Escape 清空目录查询；无匹配时显示明确空状态。
- 折叠箭头与标题导航是两个独立按钮，点击箭头不会滚动正文。
- 点击标题定位真实正文标题；上下/Home/End 移动目录焦点，左右键控制分支或导航父子项。
- 正文滚动时更新当前目录项；当前项在已折叠分支内时标记其可见祖先。
- 阅读进度依据实际 scrollTop / (scrollHeight - clientHeight) 计算，不使用设计图里的固定百分比。
- 无滚动空间且存在标题时显示 100%；无标题不显示进度。
- 切换文档清空目录查询与折叠状态。
- 当前目录来自 Muya 的实时标题数据；源码模式下目录不提供定位，隐藏阅读进度，避免操作隐藏的 WYSIWYG 表面。
- 收起目录保留活动栏，点击活动栏目录按钮可重新展开。
- 继续保留文件浏览、全局搜索、设置及面板宽度拖动/键盘调整能力。已保存的面板宽度优先于新默认值。

## 4. 毛玻璃材质

### 适用区域

顶栏、左侧导航、标签栏、工具栏及状态栏使用轻盈的磨砂玻璃材质。正文保持接近不透明的主题底色，保证长时间阅读与编辑的对比度。

### 基础参数

| 参数 | 规则 |
| --- | --- |
| 默认强调色 | 沿用主题变量 --mt-accent，浅色默认 #21b56f |
| 玻璃表面 | --mt-sidebar-bg 的 72% 与透明色混合 |
| 背景滤镜 | blur(24px) saturate(125%) |
| 玻璃边界 | 主题边线与正文底色混合，1px 细边 |
| 环境色 | 低饱和玉绿、青色、淡紫径向渐变 |
| 主面板圆角 | 16px |
| 控件圆角 | 通常 8–12px |
| 选中底色 | 强调色约 13% |

这是 **WebView 内的 backdrop-filter 毛玻璃**：模糊应用自身的环境色背景，不依赖桌面壁纸透出，也不启用系统级透明窗口或系统 Acrylic。

不能把正文、图片、代码或弹出内容整体降低透明度。深色及其他内置主题继续使用各自主题变量，不硬编码白底。浏览器不支持 backdrop-filter、用户要求降低透明度或使用强制颜色模式时，退化为清晰的不透明表面。

## 5. 标签、工具栏与正文

- 标签页保留切换、关闭、新建、中键关闭、拖动排序、键盘操作和未保存状态。
- 激活标签用正文底色、上方圆角和底部强调色细线标记。
- 未保存状态使用真实文档状态；不能为模仿设计稿给全部标签添加相同保存指示。
- 工具栏保留原有编辑操作和更多菜单。
- 编辑按钮按固定间距靠左排列，仅搜索/更多组靠右；禁止使用 space-between 拉大各编辑组之间的距离。
- 编辑区宽度 700px 及以上显示完整工具栏；820px 及以下先缩小按钮与间距，低于 700px 才收纳扩展操作，低于 460px 再收纳次要常用操作。断点依据编辑区实际宽度，不能按整个窗口宽度判断。
- 工具栏间距和断点统一由 EditorToolbar.vue 管理，玻璃主题只调整材质与外观，不覆盖响应式布局。
- **正文搜索图标必须始终作为工具栏右侧的独立按钮存在，位于更多菜单之前。** 窄窗口不能将它隐藏到更多菜单。
- 搜索打开现有查找/替换面板；面板宽度受编辑区域约束，窄窗口内允许控件换行。
- 正文沿用用户字体、字号、行高、行宽及代码字体配置；设计稿内容与章节顺序只是示例，不改写真实文档。
- 正文内边距随宽度在 24–64px 调整，保留足够留白。
- 状态栏保留真实字数、字符数、段落数、编码、换行符、保存状态与编辑模式。

## 6. 工程约束

- IPC 统一经过 src/services/tauri-invoke.ts；组件不直接调用 invoke。
- 自定义顶栏新增 cmd_popup_editor_menu，只允许 main/editor-* 编辑窗口请求合法子菜单索引与有限非负坐标。
- 菜单弹出位置按 WebView 缩放换算为逻辑坐标。
- 不修改 Muya 源码来实现应用外观；样式仅限定在应用编辑器范围。
- 不引入全局 box-sizing 重置，避免改变 Mermaid 等内容的尺寸测量。
- 定时器、滚动监听、ResizeObserver、窗口事件和键盘监听在组件卸载时释放。
- 可聚焦按钮有清晰焦点轮廓；目录使用 aria-current，折叠按钮使用 aria-expanded；界面文案覆盖中/英/日。
- 尊重 prefers-reduced-motion、prefers-reduced-transparency 和 forced-colors。
- 新增构建检查使用独立 src-tauri/target-design 目录，避免占用正在运行的已有可执行文件。

## 7. 实现入口与验收

| 入口 | 职责 |
| --- | --- |
| src/components/titleBar/TitleBar.vue | 统一顶栏、菜单入口、窗口按钮 |
| src-tauri/src/menu/mod.rs | 原生菜单生命周期及 Windows 编辑窗口装饰 |
| src-tauri/src/commands/menu.rs | 子菜单弹出命令 |
| src/components/sideBar/TocPane.vue | 目录树、搜索、折叠、定位、进度 |
| src/assets/styles/editor-glass.css | 应用级玻璃材质与布局外观 |
| src/components/editorToolbar/EditorToolbar.vue | 保留独立正文搜索按钮 |
| src/components/search/FindReplaceBar.vue | 窄编辑区内的查找/替换布局 |
| test/e2e/editor-glass.spec.ts | 真实浏览器交互与截图验证 |

验收命令：

~~~sh
npm run lint
npm run build
npm run test:unit
npm run test:e2e
cargo check --manifest-path src-tauri/Cargo.toml --target-dir src-tauri/target-design
~~~

新增浏览器用例覆盖标题栏结构、路径隐藏、目录搜索/无结果/清空、折叠状态恢复、键盘导航、正文定位、进度、目录收起恢复及 800px 深色窗口中的查找入口。截图输出位于 output/design/。

浏览器截图只证明渲染与交互；Rust 编译只证明原生代码可构建。Windows 原生菜单弹出、窗口拖动/最大化、未保存关闭提示及多窗口菜单重建需要桌面运行验收，不应把浏览器测试描述成完整 Tauri GUI 测试。
