Generated with built-in image_gen. Inputs: original design and user-provided header reference.

Edit the full MarkText desktop UI design in image 1. Image 2 is the exact visual reference for the replacement top chrome. Produce a complete high fidelity full editor screenshot, same landscape framing and same polished light jade frosted glass design, not just a header crop.
Changes: Replace the old separate title/menu bars with ONE unified approximately 56px tall title bar matching image 2: blue MarkText logo and bold MarkText left, then 文件(F) 编辑(E) 段落(P) 格式(O) 视图(V) 主题(T) 窗口(W) 帮助(H) on the SAME horizontal row, Windows minimize maximize close right. Rounded upper corners, translucent white acrylic, softly blurred mint/cyan/lavender behind. Completely REMOVE the entire file path/breadcrumb row; no C: Users jack Desktop github travel-notes path anywhere.
Directly under unified title bar: left sidebar header as in image 2 with icon, '文章目录', secondary '12 个章节', double-left collapse button; right document tabs some.md and active SKILL.md matching image 2 with green status dot and green underline, plus button far right.
Below tabs keep full formatting toolbar, including undo redo, text style, bold italic strikethrough, code, link, image, list, checklist, quote, code block, table. MUST show an unmistakable magnifying-glass SEARCH ICON at the RIGHT END OF THE FORMATTING TOOLBAR immediately before the ellipsis menu. It must not disappear with removal of the breadcrumb row. This search icon is in addition to sidebar search.
Preserve existing beautiful hierarchical left outline, H2/H3 chips, thin hierarchy guides, selected green '场景一：明确关键词搜索', search field, reading progress, article content, YAML syntax highlighting, footer. Preserve sharp readable Chinese, spacing, white reading canvas, subtle glass sidebars. Use reclaimed vertical space naturally. No explanatory annotations.
